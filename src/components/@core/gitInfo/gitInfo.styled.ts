import {defaultStitches} from '@/themes';
const {styled} = defaultStitches;

export const StitchesGitInfo = styled('div', {
  position: 'fixed',
  bottom: '0.5%',
  left: '0.5%',
  color: '$coverWhiteOpacity',
  fontSize: '10px'
});
